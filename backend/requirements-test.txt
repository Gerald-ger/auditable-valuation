# Minimum needed to run backend/tests offline.
# Not the app's runtime set — that is requirements.post-openbb.txt. yfinance is
# here only because data_provider imports it at module scope, and
# financial_models imports data_provider; the tests themselves never call it.
#
# fastapi and aiohttp are here for the same reason: test_search_and_history.py
# imports main for PERIOD_INTERVALS and _bars_per_day, and main imports fastapi
# and pydantic at module scope plus ai_client, which imports aiohttp. Without
# them collection aborts before a single test runs. pydantic arrives with fastapi.
pytest
yfinance
pandas
fastapi
aiohttp
