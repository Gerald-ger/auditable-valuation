backend/.venv/
__pycache__/
frontend/node_modules/
frontend/dist/
.pytest_cache/
# build metadata generated by `pip install -e .`
*.egg-info/
# local score history + watchlist — your data, never committed
backend/data/
.ruff_cache/
# personal working reminder, not project documentation — TODOLIST.md is the public list
NEXT-STEPS.md
# a point-in-time architecture audit with its own verification record. Same
# category as the file above: a working document, superseded as its items land,
# and not the public account of the project. TODOLIST.md remains that.
ARCHITECTURE-REVIEW-VERIFIED.md
# local Claude Code tooling, not part of the platform. settings.local.json was previously
# protected only by a machine-global ignore, so any other clone would have staged it.
**/.claude/settings.local.json
.claude/agents/
