[pytest]
testpaths = backend/tests
# Network tests are deselected by default so the suite runs offline and in CI.
# Run them deliberately with:  python -m pytest -m network
addopts = -m "not network"
markers =
    network: hits live yfinance; excluded from the default run
